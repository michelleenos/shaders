import { Uniforms } from '../types/types'

const uniforms: Uniforms = {
    u_intOffsetX: {
        value: 0.1,
        min: 0,
        max: 2,
        step: 0.01,
    },
    u_intOffsetY: {
        value: 0.1,
        min: 0,
        max: 2,
        step: 0.01,
    },
}

export default uniforms
